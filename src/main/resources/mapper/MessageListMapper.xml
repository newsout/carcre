<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sout.carcre.mapper.MessageListMapper">
<!--        public selectMessageByUserId(Integer userId);-->
    <select id="selectMessageByUserId" resultType="com.sout.carcre.controller.bean.MessageData">
        select nickname,meg_content,message_list.create_time
        from user_info,message_list
        where user_info.user_id=user_id_from and message_list.user_id=#{userId}
        order by message_list.id DESC
        limit 10
    </select>
</mapper>
